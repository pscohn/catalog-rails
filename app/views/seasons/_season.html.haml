%p
  %strong
    Season #{season.number}:
  #{season.episodes.count} episodes
  (#{link_to 'Delete', [season.show, season], method: :delete, data: {confirm: "Are you sure?"}})
%ul
  - season.episodes.each do |episode|
    %li
      %a{:href => "http://google.com/#q=#{season.show.title} season #{season.number} episode #{episode.number}"}
        Episode #{episode.number}
      \- Watched: #{episode.watched}
