%h1
  Movies
= link_to 'New Movie', new_movie_path
%h2
  Movies to Watch
%table
  %tr
    %th
      Title
    %th
      Year
    %th
      Watched
    %th{:colspan => "3"}
  - @unwatched.each do |movie|
    %tr
      %td
        = movie.title
      %td
        = movie.year
      %td
        - if movie.watched
          Yes
        - else
          No
      %td
        = link_to "Show", movie_path(movie)
      %td
        = link_to "Edit", edit_movie_path(movie)
      %td
        = link_to "Delete", movie_path(movie), method: :delete, data: {confirm: 'Are you sure?'}
%h2
  Movies Watched
%table
  %tr
    %th
      Title
    %th
      Year
    %th
      Watched
    %th{:colspan => "3"}
  - @watched.each do |movie|
    %tr
      %td
        = movie.title
      %td
        = movie.year
      %td
        - if movie.watched
          Yes
        - else
          No
      %td
        = link_to "Show", movie_path(movie)
      %td
        = link_to "Edit", edit_movie_path(movie)
      %td
        = link_to "Delete", movie_path(movie), method: :delete, data: {confirm: 'Are you sure?'}
